/**
 * Next.js Configuration
 *
 * Note: 'swcMinify' is not a valid option in recent Next.js versions. Minification is enabled by default.
 */
const nextConfig = {
  reactStrictMode: true
};

module.exports = nextConfig;
